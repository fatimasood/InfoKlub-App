class CVModel {
  final String name;
  late final String templateId;
  // Add all other CV fields

  CVModel({
    required this.name,
    this.templateId = 'default',
    // Initialize other fields
  });
}
